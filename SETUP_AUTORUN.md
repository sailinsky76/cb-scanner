# 无人值守跑批 · 装配清单

> **对应 `notify.py` v3**（Server酱 + 邮件双渠道 / 飞书卡片）与 **`daily_run.ps1` v4**。
> 改了 `notify.py` 的渠道或环境变量，回来改这份；不然下次照着配一台新机器会踩坑。
>
> `daily_run.ps1` v4 相对 v3 的行为变化，都在下面各自的章节里：
> ① **开机判据从「周末专用」提成所有关机路径的第一道闸**——见「第一道闸：这台机器是谁开的」；
> ② 有人登录着但只认出了第二路信号时，不再误用 `shutdown /s /f`——见「判定矩阵」；
> ③ `py` 不在 PATH 时不再连周末关机一起挡掉；
> ④ `-WeekendShutdownUntil` 更名 `-RtcBootBefore`（旧名仍可用，填了就覆盖新名）；
> ⑤ 休眠路（第 0 步 B 路）下开机判据不再恒为假；
> ⑥ 日志折叠进度条帧、按 `-LogKeepMonths`（默认 6）自动清理。
>
> v3 相对 v2 的四处变化仍然有效：当日标记"推送一成功就写"、周末按开机时间判、
> `shutdown.exe` 排程失败会被发现并重试、新增 `-ShutdownOnly`。

目标链路：**BIOS 定时开机 → Windows 任务计划跑 daily_run.ps1 → 手机收到通知 → 自动关机**

> **任务计划的触发器是「每天」，不是「周一至周五」。**
> BIOS RTC 只能设「每天开机」，周末没有任务来收尾的话机器会一直亮到周一。
> 所以周末照样触发，`daily_run.ps1` 里的周末分支只做一件事：确认没人在用，把机器关回去。
> 详见第 6 步与「关机决策」一节。

装配顺序**故意是倒着来的**：先把最末端的推送打通，再往前接自动开机。
反过来做的话，前几天你会分不清「机器没开」和「跑了但没推」——
而这两者的排查方向完全不同。

---

## 第 0 步：先决定走哪条路（两条路互斥，别混）

| | A. BIOS RTC 定时开机 | B. 休眠 + 任务计划唤醒 |
|---|---|---|
| 机器平时的状态 | **完全关机**（S5，零功耗） | **休眠**（S4，仍有微弱待机功耗） |
| 要不要进 BIOS | 要 | 不要 |
| 快速启动 | **必须关掉** | 保留 |
| 可靠性 | 高，但依赖主板支持 | 高，但不是真正的"关机" |

**你问的是"自动开机 + 自动关机"，那就是 A。** 下面按 A 写。
主板要是找不到 RTC 相关选项，退到 B（第 5 步末尾有说明）。

---

## 第 1 步：把两个脚本放进项目

```
C:\path\to\cb_scanner\
├── run.py              ← 一个字都没动
├── notify.py           ← 新增
├── daily_run.ps1       ← 新增
├── reports\            ← run.py 产出；notify.py 只读这里
└── logs\               ← 脚本自己建
```

`notify.py` 只用标准库（含 `smtplib` / `email`），**不用改 requirements.txt**。

---

## 第 2 步：配推送渠道（先打通这一端）

### 两个渠道，分工不同——不是互为备份

| 渠道 | 管什么 | 为什么 |
|---|---|---|
| **Server酱** | 「今天要不要动手」 | 微信横幅那一眼。待办数和退出数在**标题**里，因为正文会被截断 |
| **邮件** | 「具体怎么动手」 | HTML 报告直接渲染在**正文**里，71 条一条不少，不用点附件 |

**别把这两条当成"配两个厂商互为保险"。** 它们发的内容不一样，
挂掉一个不等于另一个能顶上——见下面"退出码约定"。

### 环境变量

填成**系统环境变量**（不是用户变量——任务计划以"不管用户是否登录"运行时读不到用户变量）：

| 变量 | 必填 | 说明 |
|---|---|---|
| `SC_SENDKEY` | Server酱要 | ⚠️ **不是 `SCKEY`**。https://sct.ftqq.com 登录后复制 SendKey |
| `SMTP_USER` | 邮件要 | 发件地址，如 `cb-scanner-xxx@126.com` |
| `SMTP_PASS` | 邮件要 | 邮箱「**客户端授权码**」，不是登录密码 |
| `SMTP_TO` | 选填 | 收件人，不填默认发给自己 |
| `SMTP_HOST` / `SMTP_PORT` | 选填 | 覆盖自动推导（126/163/QQ 都能自动推出来） |
| `BARK_URL` | 选填 | Bark（iOS），App 里那条 `https://api.day.app/xxxx` |
| `FEISHU_WEBHOOK` | 选填 | 飞书自定义机器人 |
| `DINGTALK_WEBHOOK` | 选填 | 钉钉；若开了加签再填 `DINGTALK_SECRET` |

> **这个名字和多数教程不一样，是有意的。** 老版 Server酱（sc.ftqq.com）的约定名是
> `SCKEY`，网上抄来的脚本和现成的 docker-compose 大多找的是那个。这里沿用
> `SC_SENDKEY` 是为了和你其他项目保持一致——**代价是从外面粘进来的代码会静默失灵**。
> 变量名对不上的表现不是报错，是「这个渠道没配」→ 直接跳过 → 什么都收不到。
> 配完先 `echo $env:SC_SENDKEY` 确认一眼。

### 126 邮箱：拿授权码

网页版 → 设置 → **POP3/SMTP/IMAP** → 开启 **IMAP/SMTP** → 扫码发短信 → 得到授权码。

**建单独一个客户端授权密码给扫描器用**（命名 `cb_scanner`），别复用你手机邮箱客户端那个——
要停就吊销这一个。

> ⚠️ **授权码等于整个邮箱的读权限**，不是"只能发信"的凭证。
> 而 `setx` 是明文写进注册表的，那台机器上任何以你身份运行的进程都读得到。
>
> 所以：**注册一个专用 126 小号当发件人，`SMTP_TO` 填你的主力邮箱。**
> 机器被摸了，泄露的是一个空邮箱的凭证，报告照样收得到。

```powershell
setx SC_SENDKEY "你的 SendKey"
setx SMTP_USER  "cb-scanner-xxxx@126.com"   # 专用小号，只用来发
setx SMTP_PASS  "小号的授权码"
setx SMTP_TO    "你的主力邮箱"
```

### 验一次

设完**重开一个 PowerShell 窗口**（环境变量不会进已开的窗口）：

```powershell
cd C:\path\to\cb_scanner

py -3.11 notify.py --selftest          # 不联网，验解析/飞书转换/邮件构造/发送次数
py -3.11 notify.py --code 0 --dry-run  # 不发、不吃额度，只打印要推什么
py -3.11 notify.py --code 0            # 真推
```

`--selftest` 看的是**有没有 ❌**，不是条数——末行会打印 `✅ 自检通过（N 条）`，
N 随版本变，别去记它。

`--dry-run` 那步重点看两行：配了几个渠道、「邮件正文将是全文：markdown X 字符 / HTML Y 字符」。
Y 是 0 就说明 `reports\` 里没有当天的 `.html`，邮件会退化成纯文本（见第 3 步）。

**Server酱 免费额度 5 条/天**，调试阶段一律先 `--dry-run`，别拿真推去试参数。

> 这时候如果 `reports\` 里还没有当天报告，你收到的会是
> 「⚠️ 扫描器 YYYYMMDD 异常 · 报告文件不存在」——**这是对的**，收到就算通。
> 「今日待办 + 转债退出」那两块要等第 3 步真跑过 `run.py` 才有。

**两个渠道都收到了再往下走。**

---

## 第 3 步：整条链路手动跑一遍（不关机）

```powershell
powershell -ExecutionPolicy Bypass -File C:\path\to\cb_scanner\daily_run.ps1 -NoShutdown
```

看四样：

1. `logs\autorun_YYYYMM.log` 里有完整时间线
2. `reports\scan_<今天>.md` **和** `scan_<今天>.html` 都生成了，且**不在** `_scratch\` 里
3. 微信收到 Server酱 的横幅
4. 邮箱收到全文，且**打开就看见排好版的表格**——不是点附件才看见

第 2 条里的 `.html` 必须单独确认：**它不存在时邮件照发不误**，
只是正文退化成 `<pre>` 包着的裸 markdown。不报错，所以只能靠眼睛。

> **126 的反垃圾可能拦你。** 网易对程序发的、带 HTML 和附件的信有 `554 DT:SPM` 这类退信。
> 代码里已经加了正规的 `Date` / `Message-ID` / 带显示名的 `From` 来降低命中率，
> 但**第一次真发要盯一下**。真退了，最快的解法是把 `SMTP_TO` 换成另一家（比如 QQ 邮箱），
> 126 只当发件方。

---

## 第 4 步：关掉快速启动（走 A 路必做）

Windows 的"关机"默认不是真关机，是把内核态休眠下来（hiberboot）。
这会让不少主板的 RTC 唤醒行为变得不可预测。

```powershell
# 管理员 PowerShell
powercfg /h off
```

这条会连带关掉休眠功能——**所以走 B 路的话别执行它**。
只想关快速启动、保留休眠：控制面板 → 电源选项 → 选择电源按钮的功能 →
更改当前不可用的设置 → 取消勾选「启用快速启动」。

顺手确认电源计划不会在跑批中途睡下去：

```powershell
powercfg /change standby-timeout-ac 0
powercfg /change hibernate-timeout-ac 0
```

---

## 第 5 步：BIOS 设定时开机

重启进 BIOS（开机狂按 Del 或 F2），选项名各家不同：

| 主板 | 路径 |
|---|---|
| 华硕 ASUS | Advanced → APM Configuration → **Power On By RTC** |
| 技嘉 Gigabyte | Settings → Platform Power → **RTC Wake / Resume by Alarm** |
| 微星 MSI | Settings → Advanced → Wake Up Event Setup → **Resume By RTC Alarm** |
| 华擎 ASRock | Advanced → ACPI Configuration → **RTC Alarm Power On** |

打开后设 **每天 09:00:00**（`RTC Alarm Date` 设 `Every Day` 或 `0`）。

**别去找「只在工作日开机」这个选项，绝大多数主板没有**（`RTC Alarm Date` 要么是 `Every Day`，
要么是 1–31 里的某一号，没有星期的概念）。这件事在软件侧解决：任务计划每天触发，
周末那次由脚本判断后直接关机。周末白开机三分钟，电费量级是每次 0.005 度，不值得为它进 BIOS。

> 真想做到「周末根本不开机」，只有走 B 路：`powercfg /h on` 保留休眠，平时用 `shutdown /h`
> 代替关机，任务计划触发器设周一至周五并勾「唤醒计算机运行此任务」——周末不唤醒，机器一直休眠。
> 代价是 S4 待机功耗和「不是真正关机」。已经走 A 路就别中途换，两条路的快速启动设置是冲突的。

**两个坑**：
- 有些主板的 RTC 走 **UTC**，设 09:00 实际是北京时间 17:00。
  第一次设完当天就该验证一次：把闹钟设成 5 分钟后 → 关机 → 看它是否准时亮。
- 断电后主板电池若没电，RTC 设置会丢。机器长期断总闸的话这条链路不可靠。

> **走 B 路的替代做法**：不进 BIOS，改用「休眠」代替关机
> （把 `daily_run.ps1` 里的 `shutdown /s` 改成 `shutdown /h`），
> 并在第 6 步的任务属性 → **条件** 里勾选「唤醒计算机运行此任务」。
> 注意这个勾选项**只能从睡眠/休眠唤醒，唤不醒一台真正关机的电脑**——
> 这是很多人第一次配失败的原因。

---

## 第 6 步：建任务计划

任务计划程序 → **创建任务**（不是"创建基本任务"，那个选项不全）

**常规**
- 名称：`cb_scanner 每日扫描`
- 选「**不管用户是否登录都要运行**」（会要你输一次开机密码）
- 勾「使用最高权限运行」← `powercfg /requests` 要管理员，在用检测的一路信号靠它
- 配置为：Windows 10 / 11

**触发器** → 新建
- **每天**，**09:05:00** ← 不是"每周 周一至周五"
- 比 BIOS 的 09:00 晚 5 分钟：留给开机 + 网卡拿到 IP

> **这一条是整份文档里最容易配错的地方。** 直觉上"扫描只在交易日有意义，那就设周一到周五"，
> 但 BIOS RTC 是**每天**把机器叫醒的：周六 09:00 机器亮了，09:05 没有任何任务运行，
> 于是它一直亮到周一早上——两天两夜。
>
> 正确的分工是：**开机时间表由 BIOS 定（每天），跑不跑由脚本定。**
> `daily_run.ps1` 第 1 段就判 `DayOfWeek`，周六周日不跑 `run.py`、不推送，直接进关机决策。
> 日志里会留一行 `今天是 Saturday，不跑扫描`。
>
> 已经建好了周一至周五的任务，改触发器就行，别重建（重建要重新输密码）：
> 任务 → 属性 → 触发器 → 编辑 → 把"每周"改成"每天"。命令行等价写法：
>
> ```powershell
> Set-ScheduledTask -TaskName "cb_scanner 每日扫描" `
>     -Trigger (New-ScheduledTaskTrigger -Daily -At 9:05am)
> ```
>
> 这条在任务存了开机密码时可能要你补 `-User` / `-Password`，报错就退回 GUI 改，别硬试。

**操作** → 新建
- 程序：`powershell.exe`
- 参数：`-NoProfile -ExecutionPolicy Bypass -File "C:\path\to\cb_scanner\daily_run.ps1"`
- 起始于：`C:\path\to\cb_scanner`

**条件**
- 取消勾「只有在计算机使用交流电源时才启动此任务」（台式机无所谓，但省得意外）
- 走 B 路才勾「唤醒计算机运行此任务」

**设置**
- 勾「如果过了计划开始时间，立即启动任务」← 开机慢的时候救命
- 勾「如果任务失败，按以下频率重新启动」：10 分钟 / 3 次
- 「如果任务运行时间超过」：设 1 小时（挂死时兜底）

> 上面这两个勾**加起来**意味着：一天之内这个任务被触发两三次是正常现象
> （错过的那次补跑一次 + 失败后重试最多三次）。而 Server酱 免费额度只有 **5 条/天**，
> 重复推送能把它吃光。所以脚本在推送成功后会写一个当日标记 `logs\done_YYYYMMDD.flag`，
> 再被触发时跳过扫描与推送、只做关机判断。要强制重跑：加 `-Force`。

### 可选（推荐）：第二个任务——补关机

主任务一天只有一次机会。它判「有人在用」而没关机之后，机器就一直亮着，
没人再来收。补一个只做关机决策的任务把这个洞堵上：

- 名称：`cb_scanner 补关机`
- 常规：同样选「不管用户是否登录都要运行」+「使用最高权限运行」
- 触发器：**每天 10:00**，勾「重复任务间隔 **30 分钟**，持续时间 **8 小时**」
- 操作：`powershell.exe`，参数
  `-NoProfile -ExecutionPolicy Bypass -File "C:\path\to\cb_scanner\daily_run.ps1" -ShutdownOnly`

`-ShutdownOnly` 只做在用检测 + 关机决策：**不跑 run.py、不跑 notify.py、不占 Server酱 额度**，
所以重复触发是安全的。它有两层保护：

- 工作日当天的 `done_YYYYMMDD.flag` 不存在就拒绝关机，免得它抢在主任务前面把机器关了；
- **v4 起它和主任务共用同一道开机判据**，所以你自己开的机器（无论工作日还是周末）它都不动。
  v3 没有这一条，周末那一支是直接放行的。

要不要开仍是个取舍：开了以后，一台**由 RTC 叫醒**的机器如果你锁屏去开会两小时，也会被关掉
（软关，300 秒，未保存的文档能把它顶回去）。真要占着机器就 `-Hold 8`。

## 关机决策：这台机器现在有没有人在用

任务计划每天触发之后，**"脚本会在有人坐在机器前的时候执行关机"这件事从理论风险变成了日常**：

- 周六 09:05 那次触发，你可能正好在用这台机器
- 周三 RTC 没开成，你中午自己开机，任务计划"补跑错过的那次"立刻触发
- 工作日扫描跑完那三分钟里，你走过来登录了

所以 `daily_run.ps1` 里**所有**关机路径都收在一个函数 `Invoke-ShutdownDecision` 后面，
它先过开机判据、再做在用检测，然后决定关不关、怎么关。

### 第一道闸：这台机器是谁开的（v4）

在用检测回答的是「此刻有没有人」，它回答不了「这台机器为什么是开着的」。
你中午自己开机干活、去接杯水锁了屏，在用检测判「无人」是对的，关机却是错的。

所以 v4 在在用检测**之前**加了一道闸，判据是 `Get-BootVerdict`：

| 机器这次起来的时刻 | 判定 | 动作 |
|---|---|---|
| **今天**，且早于 `-RtcBootBefore`（默认 11:00） | RTC 叫醒的 | 放行，进在用检测 |
| 今天，但晚于该时刻 | **你自己开的** | 不关机 |
| **昨天或更早**（机器一直没关过） | 上一次跑批没关成的告警状态 | 不关机——别把告警抹掉 |
| 拿不到开机时间 | —— | 退回挂钟规则（现在早于该时刻就放行） |

「起来的时刻」取 `Win32_OperatingSystem.LastBootUpTime` 与**最近一次唤醒事件**
（`Microsoft-Windows-Power-Troubleshooter` / Id 1）里更晚的那个。
只看前者的话，走 B 路（休眠 + 计划唤醒）的机器跨休眠不更新，会永远落在「昨天或更早」那一档。

三个要知道的后果：

- **RTC 设的不是 09:00，就得相应调 `-RtcBootBefore`。** 它是"RTC 开机时刻的**上界**"，
  不是"最晚允许关机的时刻"——判的是开机时间，所以迟到的那次触发照样能把机器收掉。
- **昨天没关成的机器，今天不会被自动收走**，要你手动关一次。这是有意的：
  "机器亮着"是这套自动化唯一的异常信号，自动抹掉它等于把故障藏起来。
  日志里会写 `机器从 MM-DD HH:mm 起就没关过`，扫日志时看得见。
- **BIOS 电池没电、RTC 设置丢了之后，机器从此不会被自动关。** 失败方向是"不关机"，
  安全，而且日志每天一行 `判定是你自己开的`——不是静默。

要回到 v3 行为（不管机器是谁开的都关）：加 `-ShutdownRegardlessOfBoot`。

### 判定矩阵（过了第一道闸之后）

| 检测到的状态 | 判定 | 动作 |
|---|---|---|
| 没有任何交互式桌面会话（RTC 冷启动到锁屏，没人登录过） | 无人 | `shutdown /s /f /t 120` |
| 有登录会话，但停在锁屏/登录界面 | 无人 | `shutdown /s /t 300`，**不带 `/f`** |
| 有登录会话且桌面已解锁 | **有人** | 不关机，退出码 0 |
| 多个会话 + 检测到锁屏（分不清谁锁了谁在用） | **有人** | 不关机 |
| `logs\NOSHUTDOWN.txt` 存在且未过期 | **有人** | 不关机 |
| 检测本身出错 / 拿不到信号 | **有人** | 不关机 |

两个刻意的选择：

- **默认判「有人」。** 每一条不确定的分支都倒向不关机。误关的代价是丢掉没保存的东西，
  关不掉的代价只是机器亮着——而"机器亮着"本来就是这套自动化约定好的异常信号，不多这一种。
- **锁屏那一档不带 `/f`。** 有未保存文档的程序能把关机顶回去，机器就留着亮。
  无人登录那一档才用 `/f`，因为那时候压根没有会丢的东西。

> **v3 在这里是错的，v4 修了。** 「有没有人登录」是两路信号判出来的：
> `explorer.exe` 的会话号 **或** `Win32_ComputerSystem.UserName`。而 v3 决定要不要带 `/f`
> 时只看前者（`$p.Sessions`）——于是「explorer 那一路没认出、第二路认出了登录用户」的机器
> 会掉进 `/f` 那一档，**未保存的文档直接丢**，而上面这张表白纸黑字写着它该走 300 秒软关。
> v4 让 `Get-Presence` 把 `LoggedIn` 一起返回，`/f` 只在真的没人登录时才用。
> 离线复现：同一份 `Invoke-ShutdownDecision`，喂 `Sessions=0 / LoggedIn=True / Locked=True`，
> v3 出 `force=True delay=120`，v4 出 `force=False delay=300`。

### 倒计时期间还会再检测

`shutdown /t` 的倒计时里，脚本每 5 秒复检一次。你在这段时间里解锁、或者登录进来，
它会自己执行 `shutdown /a` 撤销，日志留一行 `倒计时中检测到有人回来`。
这一条针对的是"关机对话框弹在锁屏后面 / 人回到座位先解锁才看见 / 来不及敲 `shutdown /a`"。

### 检测用的信号，以及为什么不用那两个常见做法

脚本以「不管用户是否登录」运行，跑在**会话 0**里。所以：

- ❌ `GetLastInputInfo` —— 只报调用方所在会话的空闲时间，会话 0 里读到的和你的桌面无关
- ❌ 弹窗询问 —— 会话 0 没有可见桌面，弹出来没人看得见

能跨会话拿到的是这三类：`explorer.exe` / `LogonUI.exe` 的进程与会话号、`quser.exe` 的空闲时间列、
`powercfg /requests` 的 DISPLAY 占用。`quser` 是 GBK 输出而脚本把控制台设成了 UTF-8，
中文列拿到的可能是乱码——**这是预期内的**，解析失败就返回"未知"，让判定退回保守分支。

### 手动挂起：今天别关我的机器

坐下来准备用一整天，又懒得去改任务计划：

```powershell
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -Hold 8   # 挂起 8 小时
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -Hold 0   # 取消
```

写的是 `logs\NOSHUTDOWN.txt`，里面就一行到期时间，过期后脚本自己删。
做个桌面快捷方式，目标填上面第一行，双击即可。

### 可选：登录着但人早走了也关机

默认**不**开。开了以后"已登录 + 未锁屏 + 空闲 ≥ N 分钟"也算无人：

```
... -File ...\daily_run.ps1 -IdleShutdownMin 30
```

它会额外查一次 `powercfg /requests`：有程序按住屏幕不灭（放视频、开会、投屏）就仍然判有人——
**看视频是不产生键鼠输入的**，只看空闲时间会把这种情况判成没人。
即便如此这一档仍有误关风险（比如你在读一篇长文档），所以默认关闭。

### 周末：判的是「机器几点开的」，不是「现在几点」

周末那一支要回答的问题只有一个：**这台机器是 RTC 叫醒的，还是你自己开的？**

v2 用挂钟判（11:00 之前才关），有个洞：周六那次触发一旦迟到——开机慢、Windows
在装更新、那会儿你正好在用、或者任务按"失败重启"推到了 11 点后——就再也不会
关机了，机器照样亮两天。而这正是这套东西要消灭的那种失败。

v3 改成读开机时间，**v4 把这套判据从周末分支里搬走了**——它现在是所有关机路径的
第一道闸，判据表见上面「第一道闸：这台机器是谁开的」。周末分支因此只剩两行：
打一句 `今天是 Saturday，不跑扫描`，然后调 `Invoke-ShutdownDecision`。

> **为什么非搬不可。** v3 里这段判据只写在周末主分支，而每 30 分钟一次的
> `-ShutdownOnly` 补关机任务**完全不判**——它对周末的处理是"直接放行"。
> 于是：你周六自己开机干活，10:00 的补关机 tick 一到、你正好锁屏去倒杯水，
> 机器就被收走了。同一件事两套判据、其中一套等于没判，这是这个项目反复吃亏的
> 那种「第二份手抄表」。v4 只留一份。

`-WeekendShutdownUntil` 因此更名 **`-RtcBootBefore`**（旧名仍然收，填了就覆盖新名，
不用去改已经建好的任务）。改名的理由是它现在工作日也生效，旧名词不达意。

### 验证这几条（都不会真关机）

```powershell
# 0. 改过脚本就先过一遍语法——比等到明早发现它崩了便宜得多
$e = $null
[void][System.Management.Automation.Language.Parser]::ParseFile(
    (Join-Path $PWD 'daily_run.ps1'), [ref]$null, [ref]$e)
$e      # 什么都不打印 = 语法没问题

# 1. 你正坐在机器前 → 应打印 有人=True
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -CheckOnly

# 2. 锁屏（Win+L）后，在任务计划里手动"运行"一个同样命令加 -CheckOnly 的临时任务
#    → 日志里应是 有人=False、锁屏=True
#    注意必须从任务计划跑，直接在 PowerShell 窗口里跑是会话 1，测不出会话 0 的行为

# 3. 挂起后再检测 → 应打印 有人=True、挂起=True
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -Hold 4
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -CheckOnly
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -Hold 0

# 4. 工作日、当天还没跑过时的补关机 → 应打印「不关机，把机器留给主任务」
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -ShutdownOnly

# 5. 【v4 必验】你自己开机的那天下午跑 -CheckOnly
#    → 日志里 [开机判据] 那一行应当以 user 结尾
#    报成 rtc 的话，要么 -RtcBootBefore 设得太晚（默认 11:00），
#    要么这台机器的开机时间读不到（会退回挂钟规则，日志里会说）
powershell -ExecutionPolicy Bypass -File .\daily_run.ps1 -CheckOnly

# 6. 【v4 必验】第二天早上 RTC 叫醒之后跑 -CheckOnly
#    → [开机判据] 那一行应当以 rtc 结尾。它一直报 user 的话，
#    这道闸会把每天的自动关机全挡掉——机器会天天亮着（安全方向，但不是你要的）
```

**第 2 条不验就别开自动关机。** 只验第 1 条等于只验了"有人时不关"，
而"没人时关得掉"才是这套自动化的正常路径。
**第 5、6 条是 v4 新增那道闸的两个方向**，只验一个方向等于没验。

---

## 退出码约定（决定机器该不该关机）

`notify.py` 的退出码，`daily_run.ps1` 靠它判断：

| 码 | 含义 | 机器 |
|---|---|---|
| 0 | **至少一个**渠道成功 | 关机 |
| 1 | 配了渠道但**全部**失败 | **不关机** |
| 2 | 一个渠道都没配置 | 见下 |

**注意 0 的含义是"至少一个"，不是"全部"。**
邮件挂了、Server酱 成功 → 仍然是 0 → 照常关机。日志里只留一行：

```
[notify] ⚠️ 1 个渠道失败，报告仍在 reports/ 里
```

这是有意的（报告还在盘上，明天开机能补看，而折溢价那栏隔夜本来就作废），
但代价是**邮件单独挂掉你不会被叫醒**——所以日常维护那条 grep 要跑。

**退出码 2 值得单独确认一次**：变量名写错、`setx` 后没重开窗口，都会走到这里。
去 `daily_run.ps1` 里看它拿 2 怎么办——如果和 0 一样关机，
那么"配置写错"这种最该被发现的故障反而是最安静的。

**脚本自己的退出码另算**，任务计划"上次运行结果"看的是这个：

| 码 | 什么情况 |
|---|---|
| 0 | 正常。**包括"检测到有人在用所以没关机"和"周末只关机不扫描"**——这两种都不是失败，不该触发任务重试 |
| 1 | `py` 不在 PATH 上 / 推送全失败。这两种都不关机 |
| `$scanCode` | 只在带了 `-NoShutdown` 时透传 run.py 的退出码 |

> **`-NoShutdown` + `$scanCode` 这一格咬过人。** run.py 退 1 或 2 时脚本 `exit 1/2`，
> 任务计划把它当失败 → 按「失败后重启 3 次」重试 → 每次都重新扫描并**重新推送**，
> 4 次就把 Server酱 那 5 条吃掉。v3 起当日标记 `done_YYYYMMDD.flag` 改成
> **只要推送成功就写**（不再等到关机那一步），重试进来会走「今天已经跑完」那一支，
> 只做关机判断。头一周带 `-NoShutdown` 跑的正是这条路径。

---

## 第 7 步：先跑一周不关机

把 `daily_run.ps1` 的参数临时加上 `-NoShutdown`，跑满 5 个交易日再撤掉。

要看的不是"有没有报错"，是这四条：

1. 每天**两条链路都到了**（微信横幅 + 邮件全文）。
   一天缺一条就是链路有洞，不是那天没机会
2. `reports\` 里每个交易日都有 `.md` **和** `.html`，文件名不带 `.mock`、不在 `_scratch\`
3. 日志里 `run.py 退出码 = 0`；出现过 1 或 2 的那天，看 `notify` 有没有把它推出来
4. 日志里搜一遍 `个渠道失败`——**这一条最容易漏**，因为它不影响关机
5. **过一个周末**：周六周日的日志里各应有一段"不跑扫描 + 在用检测 + 关机"，
   而 `reports\` 里不该多出周末的文件。这一条要等到第一个周末才验得了，
   在那之前别撤 `-NoShutdown`

### 告警路径必须单独验一次

**退出码 1/2 的推送必须验证过至少一次**，否则你只验证了"顺利那条路"。
直接造一条：

```powershell
py -3.11 notify.py --code 2
```

手机上应当收到 🛑「所有源都失败，这份报告没有参考价值」。**没收到就别开自动关机。**

> **别用"拔网线"来做这个测试。** 拔了网线 `run.py` 是会失败，
> 但 `notify.py` 同样发不出去——Server酱 和邮件都要联网。
> 那种情况下收不到告警是必然的，说明不了任何事。

---

## 日常维护

- 日志按月切分在 `logs\autorun_YYYYMM.log`，**v4 起超过 `-LogKeepMonths`（默认 6 个月）自动删**。
  "攒不大"这句以前是错的：一天跑三次就是 1589 行 / 159 KB，而且每一份里都是
  **实盘报告全文 + config.yaml 里的真实本金**（`.gitignore` 里那条 `logs/` 讲的就是这件事）。
  v4 另外折叠了 tqdm 的进度帧和空 stderr 记录（实测那份日志里占 207 行 / 13%），
  折叠了几行会在日志里照说一句
- `logs\done_YYYYMMDD.flag` 是"今天已推送成功"的标记，**推送一成功就写**（带不带
  `-NoShutdown` 都写），超过 10 天的脚本自己清。某天想重跑，删掉当天那个文件，
  或者直接加 `-Force`
- `logs\NOSHUTDOWN.txt` 在就说明关机被挂起了。**它过期才会被自动删**——
  如果你用 `-Hold 720` 挂了 30 天然后忘了，机器会连着 30 天不关，日志里每天一行
  `存在挂起文件，有效期至 ...`。每周扫日志时顺便看一眼这行
- 周末日志应当每天三四行（`今天是 Saturday，不跑扫描` + 一行开机时间判定 + 在用检测 + 关机）。
  **周末日志是空的 = RTC 没把机器叫醒**，和"跑失败"是两回事，别混。
  看到 `机器从 MM-DD HH:mm 起就没关过` 就去查前一个工作日的日志尾部——那天没关成
- **机器某天早上还亮着 = *所有*渠道都挂了**，去看日志尾部。
  反过来不成立：机器关了只说明*至少一个*成功，不代表邮件也到了
- 所以每周扫一次：`Select-String "个渠道失败" logs\autorun_*.log`
- 改了 `config.yaml` 之后手动跑一次 `-NoShutdown`，别让改坏的配置在无人值守下跑一周
- **别顺手关掉 126 网页版里的 IMAP/SMTP 服务**——关了授权码立刻失效，
  而失效的表现是邮件渠道静默消失（Server酱 还在，机器照常关机）
- 授权码轮换后记得 `setx` 重设并**重开窗口**，任务计划下次运行才读得到
- `check.sh` 那七项**不要**放进这个日常链路：它会往 `reports\_scratch\` 写东西、
  也和当天有没有机会无关。要跑就手动跑

---

## 这条链路仍然没解决的事

**它保证报告被生成和被送达，不保证你会去做。** 缴款和最后交易日都有 deadline，
而通知躺在手机上和没收到，结果是一样的。要闭环还差一步：
一个成交记账（日期 / 标的 / 来源栏 / 成本 / 了结价 / 实际损益）。
在那之前，"这套自动化到底赚没赚到钱"仍然是个无法回答的问题。
