# 无人值守跑批 · 装配清单

> **对应 `notify.py` v3**（Server酱 + 邮件双渠道 / 飞书卡片）。
> 改了 `notify.py` 的渠道或环境变量，回来改这份；不然下次照着配一台新机器会踩坑。

目标链路：**BIOS 定时开机 → Windows 任务计划跑 daily_run.ps1 → 手机收到通知 → 自动关机**

装配顺序**故意是倒着来的**：先把最末端的推送打通，再往前接自动开机。
反过来做的话，前几天你会分不清「机器没开」和「跑了但没推」——
而这两者的排查方向完全不同。

---

## 第 0 步：先决定走哪条路（两条路互斥，别混）

| | A. BIOS RTC 定时开机 | B. 休眠 + 任务计划唤醒 |
|---|---|---|
| 机器平时的状态 | **完全关机**（S5，零功耗） | **休眠**（S4，仍有微弱待机功耗） |
| 要不要进 BIOS | 要 | 不要 |
| 快速启动 | **必须关掉** | 保留 |
| 可靠性 | 高，但依赖主板支持 | 高，但不是真正的"关机" |

**你问的是"自动开机 + 自动关机"，那就是 A。** 下面按 A 写。
主板要是找不到 RTC 相关选项，退到 B（第 5 步末尾有说明）。

---

## 第 1 步：把两个脚本放进项目

```
C:\path\to\cb_scanner\
├── run.py              ← 一个字都没动
├── notify.py           ← 新增
├── daily_run.ps1       ← 新增
├── reports\            ← run.py 产出；notify.py 只读这里
└── logs\               ← 脚本自己建
```

`notify.py` 只用标准库（含 `smtplib` / `email`），**不用改 requirements.txt**。

---

## 第 2 步：配推送渠道（先打通这一端）

### 两个渠道，分工不同——不是互为备份

| 渠道 | 管什么 | 为什么 |
|---|---|---|
| **Server酱** | 「今天要不要动手」 | 微信横幅那一眼。待办数和退出数在**标题**里，因为正文会被截断 |
| **邮件** | 「具体怎么动手」 | HTML 报告直接渲染在**正文**里，71 条一条不少，不用点附件 |

**别把这两条当成"配两个厂商互为保险"。** 它们发的内容不一样，
挂掉一个不等于另一个能顶上——见下面"退出码约定"。

### 环境变量

填成**系统环境变量**（不是用户变量——任务计划以"不管用户是否登录"运行时读不到用户变量）：

| 变量 | 必填 | 说明 |
|---|---|---|
| `SC_SENDKEY` | Server酱要 | ⚠️ **不是 `SCKEY`**。https://sct.ftqq.com 登录后复制 SendKey |
| `SMTP_USER` | 邮件要 | 发件地址，如 `cb-scanner-xxx@126.com` |
| `SMTP_PASS` | 邮件要 | 邮箱「**客户端授权码**」，不是登录密码 |
| `SMTP_TO` | 选填 | 收件人，不填默认发给自己 |
| `SMTP_HOST` / `SMTP_PORT` | 选填 | 覆盖自动推导（126/163/QQ 都能自动推出来） |
| `BARK_URL` | 选填 | Bark（iOS），App 里那条 `https://api.day.app/xxxx` |
| `FEISHU_WEBHOOK` | 选填 | 飞书自定义机器人 |
| `DINGTALK_WEBHOOK` | 选填 | 钉钉；若开了加签再填 `DINGTALK_SECRET` |

> **这个名字和多数教程不一样，是有意的。** 老版 Server酱（sc.ftqq.com）的约定名是
> `SCKEY`，网上抄来的脚本和现成的 docker-compose 大多找的是那个。这里沿用
> `SC_SENDKEY` 是为了和你其他项目保持一致——**代价是从外面粘进来的代码会静默失灵**。
> 变量名对不上的表现不是报错，是「这个渠道没配」→ 直接跳过 → 什么都收不到。
> 配完先 `echo $env:SC_SENDKEY` 确认一眼。

### 126 邮箱：拿授权码

网页版 → 设置 → **POP3/SMTP/IMAP** → 开启 **IMAP/SMTP** → 扫码发短信 → 得到授权码。

**建单独一个客户端授权密码给扫描器用**（命名 `cb_scanner`），别复用你手机邮箱客户端那个——
要停就吊销这一个。

> ⚠️ **授权码等于整个邮箱的读权限**，不是"只能发信"的凭证。
> 而 `setx` 是明文写进注册表的，那台机器上任何以你身份运行的进程都读得到。
>
> 所以：**注册一个专用 126 小号当发件人，`SMTP_TO` 填你的主力邮箱。**
> 机器被摸了，泄露的是一个空邮箱的凭证，报告照样收得到。

```powershell
setx SC_SENDKEY "你的 SendKey"
setx SMTP_USER  "cb-scanner-xxxx@126.com"   # 专用小号，只用来发
setx SMTP_PASS  "小号的授权码"
setx SMTP_TO    "你的主力邮箱"
```

### 验一次

设完**重开一个 PowerShell 窗口**（环境变量不会进已开的窗口）：

```powershell
cd C:\path\to\cb_scanner

py -3.11 notify.py --selftest          # 不联网，验解析/飞书转换/邮件构造/发送次数
py -3.11 notify.py --code 0 --dry-run  # 不发、不吃额度，只打印要推什么
py -3.11 notify.py --code 0            # 真推
```

`--selftest` 看的是**有没有 ❌**，不是条数——末行会打印 `✅ 自检通过（N 条）`，
N 随版本变，别去记它。

`--dry-run` 那步重点看两行：配了几个渠道、「邮件正文将是全文：markdown X 字符 / HTML Y 字符」。
Y 是 0 就说明 `reports\` 里没有当天的 `.html`，邮件会退化成纯文本（见第 3 步）。

**Server酱 免费额度 5 条/天**，调试阶段一律先 `--dry-run`，别拿真推去试参数。

> 这时候如果 `reports\` 里还没有当天报告，你收到的会是
> 「⚠️ 扫描器 YYYYMMDD 异常 · 报告文件不存在」——**这是对的**，收到就算通。
> 「今日待办 + 转债退出」那两块要等第 3 步真跑过 `run.py` 才有。

**两个渠道都收到了再往下走。**

---

## 第 3 步：整条链路手动跑一遍（不关机）

```powershell
powershell -ExecutionPolicy Bypass -File C:\path\to\cb_scanner\daily_run.ps1 -NoShutdown
```

看四样：

1. `logs\autorun_YYYYMM.log` 里有完整时间线
2. `reports\scan_<今天>.md` **和** `scan_<今天>.html` 都生成了，且**不在** `_scratch\` 里
3. 微信收到 Server酱 的横幅
4. 邮箱收到全文，且**打开就看见排好版的表格**——不是点附件才看见

第 2 条里的 `.html` 必须单独确认：**它不存在时邮件照发不误**，
只是正文退化成 `<pre>` 包着的裸 markdown。不报错，所以只能靠眼睛。

> **126 的反垃圾可能拦你。** 网易对程序发的、带 HTML 和附件的信有 `554 DT:SPM` 这类退信。
> 代码里已经加了正规的 `Date` / `Message-ID` / 带显示名的 `From` 来降低命中率，
> 但**第一次真发要盯一下**。真退了，最快的解法是把 `SMTP_TO` 换成另一家（比如 QQ 邮箱），
> 126 只当发件方。

---

## 第 4 步：关掉快速启动（走 A 路必做）

Windows 的"关机"默认不是真关机，是把内核态休眠下来（hiberboot）。
这会让不少主板的 RTC 唤醒行为变得不可预测。

```powershell
# 管理员 PowerShell
powercfg /h off
```

这条会连带关掉休眠功能——**所以走 B 路的话别执行它**。
只想关快速启动、保留休眠：控制面板 → 电源选项 → 选择电源按钮的功能 →
更改当前不可用的设置 → 取消勾选「启用快速启动」。

顺手确认电源计划不会在跑批中途睡下去：

```powershell
powercfg /change standby-timeout-ac 0
powercfg /change hibernate-timeout-ac 0
```

---

## 第 5 步：BIOS 设定时开机

重启进 BIOS（开机狂按 Del 或 F2），选项名各家不同：

| 主板 | 路径 |
|---|---|
| 华硕 ASUS | Advanced → APM Configuration → **Power On By RTC** |
| 技嘉 Gigabyte | Settings → Platform Power → **RTC Wake / Resume by Alarm** |
| 微星 MSI | Settings → Advanced → Wake Up Event Setup → **Resume By RTC Alarm** |
| 华擎 ASRock | Advanced → ACPI Configuration → **RTC Alarm Power On** |

打开后设 **每天 09:00:00**（`RTC Alarm Date` 设 `Every Day` 或 `0`）。

**两个坑**：
- 有些主板的 RTC 走 **UTC**，设 09:00 实际是北京时间 17:00。
  第一次设完当天就该验证一次：把闹钟设成 5 分钟后 → 关机 → 看它是否准时亮。
- 断电后主板电池若没电，RTC 设置会丢。机器长期断总闸的话这条链路不可靠。

> **走 B 路的替代做法**：不进 BIOS，改用「休眠」代替关机
> （把 `daily_run.ps1` 里的 `shutdown /s` 改成 `shutdown /h`），
> 并在第 6 步的任务属性 → **条件** 里勾选「唤醒计算机运行此任务」。
> 注意这个勾选项**只能从睡眠/休眠唤醒，唤不醒一台真正关机的电脑**——
> 这是很多人第一次配失败的原因。

---

## 第 6 步：建任务计划

任务计划程序 → **创建任务**（不是"创建基本任务"，那个选项不全）

**常规**
- 名称：`cb_scanner 每日扫描`
- 选「**不管用户是否登录都要运行**」（会要你输一次开机密码）
- 勾「使用最高权限运行」
- 配置为：Windows 10 / 11

**触发器** → 新建
- 每周，周一至周五，**09:05:00**
- 比 BIOS 的 09:00 晚 5 分钟：留给开机 + 网卡拿到 IP

**操作** → 新建
- 程序：`powershell.exe`
- 参数：`-NoProfile -ExecutionPolicy Bypass -File "C:\path\to\cb_scanner\daily_run.ps1"`
- 起始于：`C:\path\to\cb_scanner`

**条件**
- 取消勾「只有在计算机使用交流电源时才启动此任务」（台式机无所谓，但省得意外）
- 走 B 路才勾「唤醒计算机运行此任务」

**设置**
- 勾「如果过了计划开始时间，立即启动任务」← 开机慢的时候救命
- 勾「如果任务失败，按以下频率重新启动」：10 分钟 / 3 次
- 「如果任务运行时间超过」：设 1 小时（挂死时兜底）

---

## 退出码约定（决定机器该不该关机）

`notify.py` 的退出码，`daily_run.ps1` 靠它判断：

| 码 | 含义 | 机器 |
|---|---|---|
| 0 | **至少一个**渠道成功 | 关机 |
| 1 | 配了渠道但**全部**失败 | **不关机** |
| 2 | 一个渠道都没配置 | 见下 |

**注意 0 的含义是"至少一个"，不是"全部"。**
邮件挂了、Server酱 成功 → 仍然是 0 → 照常关机。日志里只留一行：

```
[notify] ⚠️ 1 个渠道失败，报告仍在 reports/ 里
```

这是有意的（报告还在盘上，明天开机能补看，而折溢价那栏隔夜本来就作废），
但代价是**邮件单独挂掉你不会被叫醒**——所以日常维护那条 grep 要跑。

**退出码 2 值得单独确认一次**：变量名写错、`setx` 后没重开窗口，都会走到这里。
去 `daily_run.ps1` 里看它拿 2 怎么办——如果和 0 一样关机，
那么"配置写错"这种最该被发现的故障反而是最安静的。

---

## 第 7 步：先跑一周不关机

把 `daily_run.ps1` 的参数临时加上 `-NoShutdown`，跑满 5 个交易日再撤掉。

要看的不是"有没有报错"，是这四条：

1. 每天**两条链路都到了**（微信横幅 + 邮件全文）。
   一天缺一条就是链路有洞，不是那天没机会
2. `reports\` 里每个交易日都有 `.md` **和** `.html`，文件名不带 `.mock`、不在 `_scratch\`
3. 日志里 `run.py 退出码 = 0`；出现过 1 或 2 的那天，看 `notify` 有没有把它推出来
4. 日志里搜一遍 `个渠道失败`——**这一条最容易漏**，因为它不影响关机

### 告警路径必须单独验一次

**退出码 1/2 的推送必须验证过至少一次**，否则你只验证了"顺利那条路"。
直接造一条：

```powershell
py -3.11 notify.py --code 2
```

手机上应当收到 🛑「所有源都失败，这份报告没有参考价值」。**没收到就别开自动关机。**

> **别用"拔网线"来做这个测试。** 拔了网线 `run.py` 是会失败，
> 但 `notify.py` 同样发不出去——Server酱 和邮件都要联网。
> 那种情况下收不到告警是必然的，说明不了任何事。

---

## 日常维护

- 日志按月切分在 `logs\autorun_YYYYMM.log`，攒不大
- **机器某天早上还亮着 = *所有*渠道都挂了**，去看日志尾部。
  反过来不成立：机器关了只说明*至少一个*成功，不代表邮件也到了
- 所以每周扫一次：`Select-String "个渠道失败" logs\autorun_*.log`
- 改了 `config.yaml` 之后手动跑一次 `-NoShutdown`，别让改坏的配置在无人值守下跑一周
- **别顺手关掉 126 网页版里的 IMAP/SMTP 服务**——关了授权码立刻失效，
  而失效的表现是邮件渠道静默消失（Server酱 还在，机器照常关机）
- 授权码轮换后记得 `setx` 重设并**重开窗口**，任务计划下次运行才读得到
- `check.sh` 那七项**不要**放进这个日常链路：它会往 `reports\_scratch\` 写东西、
  也和当天有没有机会无关。要跑就手动跑

---

## 这条链路仍然没解决的事

**它保证报告被生成和被送达，不保证你会去做。** 缴款和最后交易日都有 deadline，
而通知躺在手机上和没收到，结果是一样的。要闭环还差一步：
一个成交记账（日期 / 标的 / 来源栏 / 成本 / 了结价 / 实际损益）。
在那之前，"这套自动化到底赚没赚到钱"仍然是个无法回答的问题。
